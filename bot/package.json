{
  "name": "neoma-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Auto-finalization bot for neoma Opinion Markets",
  "scripts": {
    "start": "npx tsx autoFinalizer.ts",
    "build": "npx tsc -p tsconfig.json",
    "start:built": "node dist/autoFinalizer.js"
  },
  "dependencies": {
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "~5.9.3"
  }
}
